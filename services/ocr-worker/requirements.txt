fastapi==0.111.0
uvicorn[standard]==0.30.1
pytesseract==0.3.10
Pillow==10.3.0
# Primary PDF engine used by main.py (extracts the embedded text layer without
# needing Tesseract; falls back to Tesseract only for image-only pages).
pymupdf==1.24.9
# Fallback rasteriser (requires the poppler-utils system package).
pdf2image==1.17.0
boto3==1.34.144
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.7.4
